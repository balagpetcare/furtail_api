# Global hardening (http context). Server blocks add response headers via snippets/security-headers.conf

server_tokens off;

# Custom 429 page (optional)
# limit_req_status 429;
# error_page 429 /429.json;
