-- Substantive DDL moved to `20260429120000_warehouse_enterprise_po_allocation_pick_pod`
-- (`pick_lists` is created there; lexicographically this migration runs earlier).
-- Keep this directory for `_prisma_migrations` identity and deterministic ordering.
SELECT 1;
