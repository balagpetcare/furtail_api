import { PrismaClient } from "@prisma/client";

/**
 * ClinicalItemCategory model was removed from the Prisma schema.
 * This seeder is kept as a no-op to avoid breaking seed.ts import order.
 */
export default async function seedClinicalItemCategories(_prisma: PrismaClient) {
  // eslint-disable-next-line no-console
  console.log("⏭️  seedClinicalItemCategories: model removed from schema — skipping.");
}
