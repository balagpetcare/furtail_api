import { PrismaClient } from "@prisma/client";

/**
 * MasterClinicalCatalogCategory / MasterClinicalCatalogItem / MasterClinicalCatalogTemplate models
 * were removed from the Prisma schema. This seeder is kept as a no-op.
 */
export default async function seedMasterClinicalCatalog(_prisma: PrismaClient) {
  // eslint-disable-next-line no-console
  console.log("⏭️  seedMasterClinicalCatalog: models removed from schema — skipping.");
}
